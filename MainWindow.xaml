<ui:FluentWindow x:Class="PommeBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:PommeBar"
        mc:Ignorable="d"
        Title="PommeBar" Height="70" Width="420"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <ui:FluentWindow.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="MenuStartup" Header="Windows ile Birlikte Başlat" IsCheckable="True" Click="MenuStartup_Click" />
            <Separator />
            <MenuItem Header="GitHub (blosny/PommeBar)" Click="MenuGitHub_Click" />
            <MenuItem Header="Çıkış" Click="MenuExit_Click" />
        </ContextMenu>
    </ui:FluentWindow.ContextMenu>

    <Grid>
        <Grid Margin="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Album Art -->
            <Border Width="46" Height="46" CornerRadius="8" Background="#20FFFFFF" VerticalAlignment="Center">
                <Grid>
                    <!-- Fallback placeholder removed due to icon missing -->
                    <Image x:Name="AlbumArtImage" Stretch="UniformToFill">
                        <Image.Clip>
                            <RectangleGeometry Rect="0,0,46,46" RadiusX="8" RadiusY="8"/>
                        </Image.Clip>
                    </Image>
                </Grid>
            </Border>

            <!-- Song Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                <TextBlock x:Name="SongTitleText" Text="Müzik Bekleniyor..." FontWeight="SemiBold" FontSize="15" Foreground="White" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ArtistNameText" Text="PommeBar" FontSize="13" Foreground="#AAFFFFFF" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
            </StackPanel>

            <!-- Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:Button x:Name="BtnPrevious" Appearance="Transparent" Icon="{ui:SymbolIcon Previous24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnPrevious_Click" />
                <ui:Button x:Name="BtnPlayPause" Appearance="Transparent" Icon="{ui:SymbolIcon Play24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnPlayPause_Click" />
                <ui:Button x:Name="BtnNext" Appearance="Transparent" Icon="{ui:SymbolIcon Next24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnNext_Click" />
                <ui:Button x:Name="BtnHeart" Appearance="Transparent" Icon="{ui:SymbolIcon Heart24}" Foreground="#FF3B30" Padding="8" Cursor="Hand" Click="BtnHeart_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
