<Window x:Class="PommeBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:PommeBar"
        mc:Ignorable="d"
        Title="PommeBar" Height="70" Width="430"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseWheel="Window_MouseWheel">
    
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="📍 Konumu Ayarla..." Click="MenuChoosePos_Click" />
            <MenuItem Header="Hızlı Konum">
                <MenuItem Header="Sağ (Saat Yanı)" Click="MenuPosRight_Click" />
                <MenuItem Header="Orta (İkonlar Yanı)" Click="MenuPosCenter_Click" />
                <MenuItem Header="Sol (Başlat Yanı)" Click="MenuPosLeft_Click" />
            </MenuItem>
            <Separator />
            <MenuItem x:Name="MenuStartup" Header="Windows ile Birlikte Başlat" IsCheckable="True" Click="MenuStartup_Click" />
            <MenuItem Header="GitHub (blosny/PommeBar)" Click="MenuGitHub_Click" />
            <MenuItem Header="Çıkış" Click="MenuExit_Click" />
        </ContextMenu>
    </Window.ContextMenu>

    <Border CornerRadius="16" Background="#EB1C1C1E" BorderBrush="#35FFFFFF" BorderThickness="1" Margin="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Opacity="0.4" Color="Black" />
        </Border.Effect>
        <Grid Margin="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Album Art -->
            <Border Width="46" Height="46" CornerRadius="8" Background="#20FFFFFF" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="SongInfo_Click">
                <Grid>
                    <ui:SymbolIcon x:Name="AlbumArtPlaceholder" Symbol="MusicNote224" FontSize="22" Foreground="#66FFFFFF" />
                    <Image x:Name="AlbumArtImage" Stretch="UniformToFill" Visibility="Collapsed">
                        <Image.Clip>
                            <RectangleGeometry Rect="0,0,46,46" RadiusX="8" RadiusY="8"/>
                        </Image.Clip>
                    </Image>
                </Grid>
            </Border>

            <!-- Song Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0" Cursor="Hand" MouseLeftButtonDown="SongInfo_Click">
                <TextBlock x:Name="SongTitleText" Text="Müzik Bekleniyor..." FontWeight="SemiBold" FontSize="15" Foreground="White" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ArtistNameText" Text="PommeBar" FontSize="13" Foreground="#AAFFFFFF" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
            </StackPanel>

            <!-- Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:Button x:Name="BtnPrevious" Appearance="Transparent" Icon="{ui:SymbolIcon Previous24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnPrevious_Click" />
                <ui:Button x:Name="BtnPlayPause" Appearance="Transparent" Icon="{ui:SymbolIcon Play24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnPlayPause_Click" />
                <ui:Button x:Name="BtnNext" Appearance="Transparent" Icon="{ui:SymbolIcon Next24}" Padding="8" Margin="0,0,4,0" Cursor="Hand" Click="BtnNext_Click" />
                <ui:Button x:Name="BtnHeart" Appearance="Transparent" Icon="{ui:SymbolIcon Heart24}" Foreground="#FF3B30" Padding="8" Cursor="Hand" Click="BtnHeart_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
