<Window x:Class="PommeBar.PositionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="PommeBar Ayarlar" Width="490"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    <Border CornerRadius="16" Background="#F01C1C1E" BorderBrush="#40FFFFFF" BorderThickness="1" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="3" Direction="270" Opacity="0.5" Color="Black" />
        </Border.Effect>
        <Grid Margin="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="PommeBar Görünüm ve Ayarlar" FontWeight="Bold" FontSize="16" Foreground="White" HorizontalAlignment="Center" />
            <TextBlock Grid.Row="1" Text="Görünüm stili, boyut ve medya kaynağı tercihlerini belirleyin" FontSize="12" Foreground="#B0FFFFFF" HorizontalAlignment="Center" Margin="0,3,0,12" />

            <!-- 1. Appearance Mode Selection -->
            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="BtnModeFloating" Content="Ada Modu (Çubuk Üstü)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnModeFloating_Click" />
                <ui:Button Grid.Column="1" x:Name="BtnModeEmbedded" Content="Görev Çubuğuna Entegre" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnModeEmbedded_Click" />
            </Grid>

            <!-- 2. Size Preset Selection -->
            <UniformGrid Grid.Row="3" Columns="3" Margin="0,0,0,10">
                <ui:Button x:Name="BtnSizeCompact" Content="Kompakt (360px)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnSizeCompact_Click" />
                <ui:Button x:Name="BtnSizeStandard" Content="Standart (430px)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnSizeStandard_Click" />
                <ui:Button x:Name="BtnSizeWide" Content="Geniş (500px)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnSizeWide_Click" />
            </UniformGrid>

            <!-- 3. Position Selection -->
            <UniformGrid Grid.Row="4" Columns="3" Margin="0,0,0,10">
                <ui:Button x:Name="BtnLeft" Content="Sol (Başlat)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnLeft_Click" />
                <ui:Button x:Name="BtnCenter" Content="Orta (İkonlar)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnCenter_Click" />
                <ui:Button x:Name="BtnRight" Content="Sağ (Saat Yanı)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnRight_Click" />
            </UniformGrid>

            <!-- 4. Media Source Filter -->
            <Grid Grid.Row="5" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="BtnFilterMusic" Content="Sadece Apple Music &amp; Spotify" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnFilterMusic_Click" />
                <ui:Button Grid.Column="1" x:Name="BtnFilterAll" Content="Tüm Medya (YouTube Dahil)" Margin="3" Padding="8" HorizontalAlignment="Stretch" Cursor="Hand" Click="BtnFilterAll_Click" />
            </Grid>

            <!-- 5. Actions -->
            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="1" Content="İptal" Appearance="Transparent" Padding="14,6" Margin="0,0,8,0" Cursor="Hand" Click="BtnCancel_Click" />
                <ui:Button Grid.Column="2" x:Name="BtnApply" Content="Uygula" Appearance="Primary" Padding="20,6" Cursor="Hand" Click="BtnApply_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
